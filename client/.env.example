# GlitchTip / Sentry DSN for frontend error tracking
# Get from: GlitchTip → your project → Settings → Client Keys (DSN)
# Must be set at BUILD time (Vite bakes it in) — set in server .env before running docker build
VITE_SENTRY_DSN=
